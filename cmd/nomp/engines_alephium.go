package main

// The Alephium (Blake3) engine is pure Go (lukechampine.com/blake3 + a small
// binary protocol client), so it is built into the default binary.
import _ "github.com/mining-pool/not-only-mining-pool/engine/alephium"
